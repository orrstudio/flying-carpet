// Central config file — редактируй значения здесь.
// В комментариях указано, за что отвечает каждая опция и какие диапазоны/эффекты ожидать.
export default {
  // Описание пути (траектории), по которой летит «ковёр» и размещаются облака
  path: {
    // Если true — путь генерируется синусоидальной функцией генератора.
    // Если false — используется массив `points` с явными {x,y,z} точками.
    useGenerate: true,

    // Длина пути в условных единицах (влияет на координату Z и общую масштабируемость).
    // Рекомендуется от ~200 до нескольких тысяч для длинных сцен.
    length: 4000,

    // Число контрольных точек/сегментов для генератора (чем больше — тем более детальная форма).
    // Это не равно числу отрисовочных сегментов линии (см. visual.lineSegments).
    segments: 15,

    // Амплитуда боковых смещений (по X) генерируемого пути.
    // Увеличение приводит к более резким поворотам.
    lateralAmplitude: 80,

    // Амплитуда вертикальных колебаний (по Y) генерируемого пути.
    verticalAmplitude: 50,

    // Сид для детерминированной генерации (0..), измените, чтобы получить другой узор.
    seed: 0,

    // Альтернатива генерации: если `useGenerate=false`, заполните массив точками
    // вида [{ x: 0, y: 1.2, z: 0 }, ...] — они будут преобразованы в CatmullRomCurve3.
    points: []
  },

  // Параметры облаков и их размещения вдоль пути
  clouds: {
    // Общее число облачных групп (cloud groups) вдоль пути.
    // Каждая группа состоит из `particlesPerCloud` «пухов» (инстансов).
    cloudCount: 400,

    // Сколько инстансов/пухов в каждой облачной группе.
    // Больше значение = более плотные объёмные облака, но дороже по отрисовке.
    particlesPerCloud: 1,

    // Ширина разброса облаков по бокам от пути (влево/вправо).
    // Чем больше — тем дальше от центра пути могут появляться облака.
    width: 50,

    // Смещение влево/вправо (-1 .. 1). Полезно для смещения всех облаков в одну сторону.
    bias: 0,

    // Минимальная относительная дистанция облака от центра пути по оси X.
    // Значение в диапазоне 0..1. Облака будут располагаться на расстоянии не менее
    // `minOffset * width` от центра (вдоль правого/левого вектора пути).
    // Установите `0.0`, если хотите допустить центр.
    minOffset: 0.5,

    // Путь к модели облака. Может быть строкой или массивом строк.
    // - Если строка `'/assets/models/cloud.glb'`, автоматически будут проверены варианты
    //   `cloud-1.glb`, `cloud-2.glb`, `cloud-3.glb` (если они есть) и использованы как варианты вида.
    // - Если нужно явно указать варианты, передайте массив:
    //   modelUrl: ['/assets/models/cloud.glb','/assets/models/cloud-1.glb', ...]
    modelUrl: '/assets/models/cloud.glb'
  },

  // Визуальные/управляющие параметры поведения камеры, линии и взаимодействий
  visual: {
    // Сколько сегментов использовать для отрисовки линии пути (чем больше — тем плавнее).
    lineSegments: 10,

    // Чувствительность прокрутки колеса для изменения позиции вдоль пути.
    // Меньшее значение — медленнее "листает" путь; увеличьте для длинных путей.
    wheelSensitivity: 0.00001,

    // Чувствительность для тач/свайп управления.
    touchSensitivity: 0.00001,

    // Сила отклонения взгляда/мышиного "look" (смещение цели и камеры локально по правому/вверх вектору).
    mouseLookStrength: 5.5,

    // Плавность следования камеры к целевой позиции (0..1). Чем ниже — тем «тяжелее» камера.
    cameraLerp: 0.25,

    // Параметры «следования» камеры по дороге (см. logic в `main.js`):
    // `cameraFollowDistance` — как далеко назад от точки на пути ставить камеру (по касательной).
    cameraFollowDistance: 6,

    // `cameraHeightOffset` — вертикальное смещение камеры относительно точки на пути.
    cameraHeightOffset: 1.6,

    // `lookAheadT` — насколько вперёд по нормализованной кривой смотреть (маленькая положительная величина).
    // Используется для расчёта точки, на которую смотрит камера (lookAt).
    lookAheadT: 0.02
  },

  // Параметры фоновой сцены/неба
  sky: {
    // Если true — центр небесной сферы постоянно совпадает с позицией камеры
    // (удобно при больших сценах, чтобы не было артефактов при отдалении).
    autoFollowCamera: true,

    // Базовый радиус сферы неба. Важно: мы не масштабируем геометрию неба,
    // вместо этого регулируем `camera.far` по длине пути.
    radius: 600,

    // Минимальный и максимальный примемлемый радиус, используемые для расчётов.
    minRadius: 600,
    maxRadius: 2000
  }
};